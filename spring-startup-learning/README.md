# Spring 启动过程学习模块

配套文档在 `docs/spring-startup-learning.md`，建议边跑边断点阅读。

运行（JDK 17+）：

```bash
JAVA_HOME=$(/usr/libexec/java_home -v 21) PATH="$JAVA_HOME/bin:$PATH" \
  ./mvnw -f spring-startup-learning/pom.xml spring-boot:run
```

